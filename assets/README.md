## assets

Holds static data and media used by the app.

- `constants/` — JSON mappings for physical constants and LaTeX symbol labels.
- `formulas/` — Formula definitions grouped by category (energy band, DOS, carrier concentration, transport, etc.).
- `fonts/`, `images/`, and other media (if present) — UI assets loaded by Flutter.
