## lib

Main application code.

- `core/` — Solvers, formula definitions, constants, and supporting logic. See `core/README.md`.
- `ui/` — Flutter widgets, controllers, and pages for the user interface. See `ui/README.md`.
- `models/` — Shared data models (e.g., workspace state, unit prefs).
